version: '3.8'

services:
  nginx:
    build: ./nginx
    ports:
      - '80:80'
    depends_on:
      - next-app

  next-app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
